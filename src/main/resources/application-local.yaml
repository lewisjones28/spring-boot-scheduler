spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scheduler
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    com.spring.scheduler: DEBUG

